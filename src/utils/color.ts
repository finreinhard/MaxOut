const background = '#1E1E1E';
const foreground = '#333';
const text = '#fff';
const primary = '#ff9500';

const colors = {
    background,
    foreground,
    text,
    primary,
};

export default colors;

